<br><br>
<div class="container-fluid p-0" style="height: 100vh; display: flex; justify-content: center; align-items: center; background-image: url('https://images.unsplash.com/photo-1511871893393-82e9c16b81e3?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1170&q=80'); background-size: cover; background-repeat: no-repeat;">
<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="text-center p-2">
        <h1>Appointments</h1>
      </div>
<table class="table w-auto">
  <thead>
    <tr>
      <th>Date</th>
      <th>User</th>
      <th>Agent</th>
    </tr>
  </thead>
 <tbody>
    <% @appointments.each do |appointment| %>
      <tr>
        <td style="font-size: 18px; padding: 10px; color: blue;"><%= appointment.date.strftime("%m/%d/%Y") if appointment.date? %></td>
        <td style="font-size: 18px; padding: 10px; color: blue;"><%= appointment.user.first_name %></td>
        <td style="font-size: 18px; padding: 10px; color: blue;"><%= appointment.account.first_name %></td>
        <td style="font-size: 18px; padding: 10px;"><%= link_to "Edit", edit_appointment_path(appointment), style: "text-decoration: none;" %></td>
        <td style="font-size: 18px; padding: 10px;"><%= button_to "Delete", appointment_path(appointment), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<% if user_signed_in? %>
<%= link_to "New Appointment", new_appointment_path %>
<% end %>
</div>
</div>
</div>
</div>


<style>
  table {
    margin: 0 auto;
  }
</style>